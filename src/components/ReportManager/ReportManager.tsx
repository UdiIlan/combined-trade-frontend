import * as React from 'react';
const styles = require('./styles.scss');


export default class ReportManager extends React.Component<any, any> {

    render() {
        return (
            <div className={styles.reportManager}>
            </div>
        );
    }
}