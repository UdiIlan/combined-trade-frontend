import * as React from 'react';
const styles = require('./styles.scss');

export default class NewTradeDialog extends React.Component<any, any> {
  render() {
    return (
      <div className={styles.newTradeDialog}>

      </div>
    );
  }
}